*******************************************************************************
●ドキュメント種別      ：各サンプル説明ファイル
●ファイル記号名称      ：manmpg.txt
●対象ライブラリ記号名称：MPG
●対象ライブラリ名称    ：MPEG
●バージョン　　        ：1.1
●作成者                ：H.T.
●作成日                ：1996-02-21
●最終修正者            ：K.K
●最終修正日            ：1996-9-27
●その他のメッセージ    ：なし
*******************************************************************************

1.動作内容
  (1) "SEGASMP\MPEG\smpmpg1"(main.c)
　　　ビデオＣＤをオーディオ・ビデオ同期再生します。
　　　１曲目（トラック２）を無限繰り返しします。

2.修正履歴 Ver.2.00 -> Ver.2.01
  サウンドドライバと共存するための対応例を付け加えました。
  具体的には、InitHW()内のサウンドブロックの設定を以下のように変更します。

          /* サウンドブロックの設定 */
        static void     setScsp(void)
        {
        #if 0
            /*  SCSPの設定  */
            *((Uint16 *)0x25b00216) = 0x00ff;
            *((Uint16 *)0x25b00236) = 0x00ef;
            *((Uint16 *)0x25b00400) = 0x000f;
        #else
            /*  SND I/F 関数による設定  */
            /*  注意：実際には、ループ回数の上限を決めること */
            while (SND_SetCdDaLev(6, 6) == SND_RET_NSET);
            while (SND_SetCdDaPan(-15, 15) == SND_RET_NSET);
            while (SND_SetTlVl(15) == SND_RET_NSET);
        #endif
            return;
        }

********************************* end of file *********************************
