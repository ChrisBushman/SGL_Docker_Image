*******************************************************************************
●ドキュメント種別      ：各ライブラリ説明ファイル
●対象ライブラリ記号名称：dma
●対象ライブラリ名称    ：DMA
●バージョン            ：1.07
                          ※Ver1.02からVer1.03は個別対応で配布したものです。
●作成者                ：N.T
●作成日                ：1994-11-11
●最終修正者            ：A.H.
●最終修正日            ：1997-05-19
●その他のメッセージ    ：なし
*******************************************************************************
1. 修正履歴

1.1. 現バージョン以前の修正・禁止事項について

1.1.1. Ver.1.00からVer.1.02への変更点

       a)不良対策
         3rdSTEPの不良の対策を以下のように行いました。以下の不良は何れも
         SCU-DMAです。

        1)sega_dma.libへのデバッグプログラム混在、デバッグ情報付加の不良対策
          ・sega_dma.libへ誤ってデバッグプログラムを混在させていました。この
            デバッグプログラムが6060000番地付近にWRITEしていました。また、
            コンパイルオプションに/debを指定していたためデバッグ情報が付加され、
            sega_dma.libが必要以上に大きくなっていました。

        2)修正点
          ・dma.bat dma.mkを正しく修正しました。
          ・リコンパイルを行いました。

        b)ハード的に使用禁止の割り込みステータスレジスタを使用している
          不良対策
            ＜不良内容＞
            ハード的に使用禁止の割り込みステータスレジスタをライブラリ内で使用
            していました。（割り込みステータスレジスタへのWRITEのタイミングに
            よっては、割り込み信号をとれない場合があります。これはハード的に使
            用禁止です。）

            ＜対策方法＞
            ライブラリ内で割り込みステータスレジスタを使用しないようにしました
            。代替えとして割り込み処理を使用するようにしました。
            これによりDMAライブラリに以下の仕様変更が発生しました。
            　DMA SCUライブラリでは、DMA終了割り込み処理を使用するため、V-BLANK
            IN割り込み処理内等、DMAより優先順位の高い割り込み処理内でDMA SCUを
            実行すると割り込み優先順位の高い割り込み処理の終了までDMAの終了を待
            ってしまいます。この待ちを防ぐ方法は次の2つの方法があります。
                (1)SCU DMAはメイン処理で行う。
                (2)割り込み優先順位を変更する。

            (2)の割り込み優先順位の推奨方法は別途ご連絡いたします。

            ＜仕様変更内容＞
            (1)SCU DMA高水準
                次の関数仕様を追加しました。

                    +-----------------------------------------------------+
                    | 関数仕様 | SCU DMA初期化  | DMA_ScuInit             |
                    +-----------------------------------------------------+
                    書式    void DMA_ScuInit(void)
                    入力    なし
                    出力    なし
                    関数値  なし
                    機能    SCU DMAの初期化をします。DMA_ScuCopyMem(),
                    DMA_ScuResult()を実行する前に必ず行ってください。
                    !!注意!!
                            SCU DMAでは、DMAモード0の転送終了割り込み処理を使
                            用しています。よって、DMAモード0の転送終了割り込み
                            の割り込みマスクレジスタは、変更しないでください。

            (2)SCU DMA低水準
                次の関数仕様、データ仕様を追加しました。

                    +-----------------------------------------------------+
                    | 関数仕様 | DMAステータス取得 | DMA_ScuGetStatus     |
                    +-----------------------------------------------------+
                    書式    void DMA_ScuGetStatus(DmaScuStatus *status,
                                                                Uint32 ch)
                    入力    ch  ：入力チャネル
                    出力    status ：ステータスポインタ
                    関数値  なし
                    機能    指定されたDMAチャネルのステータスを取得します。
                    !!注意!!
                            2ndSTEPに同様の関数がありました。しかし本バージョ
                            ンとでは以下の違いがあります。

                            DmaScuStatus構造体のメンバ、dxbk（DMA中断中フラグ
                            ）・dxwt（DMA待機中フラグ）を削除しました。

                    +-----------------------------------------------------+
                    | データ仕様 | ステータス        | DmaScuStatus       |
                    +-----------------------------------------------------+
                    ステータスは以下のような構造体です。

                    struct {
                        Uint32 dxmv;
                    }

                    以下に、この各メンバで使用できる定数を示します。

                        dxmv /* DMA動作フラグ */
                        +-------------------------------------------------+
                        |定数           |説明                             |
                        +-------------------------------------------------+
                        | DMA_SCU_MV    |動作中                           |
                        | DMA_SCU_NO_MV |動作中でない                     |
                        +-------------------------------------------------+

1.1.2. Ver.1.02からVer.1.03への変更点

       a)不良対策
         Ver1.02(1994-09-30)の不良対策を以下のように行いました。

         1) 共通
          ・マニュアル記述のDMAパラメータのマスク項目内容が誤っていた。

            ＜不良内容＞
            マニュアルには「DMAのマスク定数を指定したビットはレジスタへの設定
            をしない。」と誤って記述していた。

            ＜対策方法＞
            正しくは、「DMAのマスク定数を指定しなかったビットはレジスタへの設定
            をしない。」である。

         2) 高水準SCU DMA
          ・DMA_ScuResult()をDMA_ScuMemCopy()の前に実行しても正しい値を
            返さない。

            ＜不良内容＞
            DMA_ScuResult()をDMA_ScuMemCopy()の前に実行しても正しい値を
            返さない。

            ＜対策方法＞
            正しい値を返すように修正した。

    b)低水準SCU DMA
         1) DMA終了割り込みのベクタを元に戻していない。
            ＜不良内容＞
            DMAの開始時にDMA終了割込みのベクタを退避している。しかしDMA終了後
            に退避しているベクタを再登録していなかった。

            ＜対策＞
            各チャネルDMA終了割込み処理中で退避しているベクタを再登録した。

         2) DMAパラメータのマスクが正しく指定できない。
            ＜不良内容＞
            DMAのパラメータのマスク定数の設定に関係なく、DXR（読み込みアドレス
            レジスタ）への設定を無条件に行うようになっていた。

            ＜対策方法＞
            マスク定数を正しく変更した。

         3) DMA_ScuAllStopはハード的に使用できないのにライブラリに存在していた

            ＜不良内容＞
            DMA_ScuAllStopで使用しているDMA強制終了レジスタはSCUのハードウェア
            制限により使用禁止であるにもかかわらず、存在していた。

            ＜対策方法＞
            この関数を削除した。

         4) DMA_ScuStopはハード的に使用できないのにライブラリに存在していた。
            ＜不良内容＞
            DMA_ScuStopはハード的な機能が存在しないのにもかかわらず、存在して
            いた。

            ＜対策方法＞
            この関数を削除した。

    c)低水準CPU DMA
         1) DMAパラメータのマスクが正しく指定できない。
            ＜不良内容＞
            DMAの共通パラメータのマスク定数の設定に関係なく、PR（プライオリテ
            ィモード）への設定を無条件に行うようになっていた。又、DMAのパラメ
            ータのマスク定数の設定に関係なく、SAR（DMAソースアドレス）への設定
            を無条件に行うようになっていた。

            ＜対策方法＞
         2) マスク定数を正しく変更した。

    d) マニュアルへの追加

         1)低水準SCU DMA
         2)低水準SCU DMAを使用する場合、使用するチャネルに対応したDMA終了
           割込みマスクレジスタを許可すること

    e) サンプルプログラムの修正
       本バージョンで動作するように変更した。
        smpdma10.c
        smpdma12.c


  1.1.3. ドキュメント追加情報  ！！ 重要  ！！(1996-09-27)
          ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
       3ndSTEP(94-08-22) -> 3rdSTEP(1994-11-11) 変更点
                           制限事項の追加 (1996-09-27)
      [禁止事項の追加]
      ＜SCU-DMA間接モードに以下の禁止事項を追加＞
      CDバッファからの読み出しに、SCU-DMAの間接モードの使用を禁止します。
      SCU-DMAの直接モードか、CPU-DMA又はソフトウェアによる転送を行って下
      さい。
      CDバッファだけでなく、A-BUS空間をソースとする転送には、ソースの読み
      込みアドレス加算値に、「4バイト加算」のみが指定可能で「加算しない」
      の設定は禁止です。
      SCU-DMAの直接モードについてはこの制限はありません。

      →関連情報  ●テクニカルインフォメーション STN-10追加事項
                    ＜仕様変更情報　No.37＞として追加

    ・理由
      SCU-DMAの間接モードにて、読み出しアドレス加算値を０にした場合に
      転送ソースがA-BUSの時に不具合が発生する為

      ライブラリ内では、上記のチェックを行っていません。
      必ず上記の制限を守って、DMAライブラリを使用して下さい。


  1.1.4. Ver.1.03からVer.1.04への変更点

    ・コンパイルオプションの変更
      ライブラリのコンパイルオプションの/SPを/NOSPへ変更しました。理由は、/SP
      でコンパイルすると関数がインライン展開されてしまうのでデバッグ時に混乱
      を招く恐れがあるためです。

    ・バージョン1.04 への移行作業時の注意

                                 ！！重要！！

        ヘッダファイルを変更していますので、ヘッダファイルをインクルードしてい
        るアプリケーションおよびライブラリは、必ずリコンパイルして下さい。
                                              ~~~~~~~~~~~~~~~~~~~~~~~~~~
  1.1.5. Ver.1.04からVer.1.05 への変更点

    ・DMA要求/応答選択制御レジスタへの外部リクエスト指定の禁止への対策。
      ライブラリでは指定が可能なので、以下のように修正。
       a) ヘッダファイルsega_dma.hからDMA_CPU_DREQマクロを削除。
       b) ファイルdma_cpu0.cのレジスタ設定関数DMA_CpuSetPrm内部で引き数の
          チェックを行い、DMA_CPU_RXIとDMA_CPU_TXI以外は設定しないように修正。


2.FAQ集
Q1
	SCU-DMAの、アドレス更新Bit とアドレス加算値の違いが良く分かりません。
A1
	アドレス更新Bit を設定すると、1回の SCU-DMAが終了した際にソースアドレスと
	ディスティネーションアドレス値が、DMA転送終了時の次のアドレスに更新されま
	す。一回の SCU-DMAでは転送しきれないデータを複数回の DMA起動によって転送
	する場合に有効な機能です。

	もう一方のアドレス加算値は、DMA転送中のアドレスが何バイトづつ更新されるか
	を設定する為のレジスタです。
	転送先の領域によって設定可能な組み合わせが異なりますので、注意して下さい。

Q2
	SCU-DMA を使用する際の注意点は？
A2
	サターンには、A-BUS,B-BUS,CPU-BUS の３つのバスがありますが、
	SCU-DMA では、同一バス間での転送はできません。(SCU-DSP の DMAも同様)
	・A-BUS：CDブロック,カートリッジ端子,MPEGデコーダ(オプション)
	・B-BUS：VDP1,VDP2,SCSP
	・CPU-BUS：WORKRAM-HIGH,WORKRAM-LOW,SMPC,BOOT-ROM,内蔵BACKUP-RAM
	     ※CPU-BUSでは、WORKRAM-HIGH以外は SCU-DMAの使用不可

	各BUS 毎の具体的な制限事項は次のようになります。

	・CPU-BUS(WORKRAM-HIGH)と、それ以外の BUS間での SCU-DMA転送中
	     SCU-DMAのバスアクセスが、SH2よりも優先され、SH2と DMAが同一バスに
	     アクセスしようとすると、SH2側が SCU-DMA転送が終了するまで WAIT状態
	     になります。(SH2内蔵キャッシュ内ならば動作可能です)
	・A-BUSとB-BUS間での DMA転送中
	     DMAの動作中に、SH2 が平行して別の処理を実行可能です。しかし、
	     CPU-BUS 以外へのアクセスを行うと、システム全体が「ハングアップ」
	     する恐れがあります。
	     A-BUS,B-BUS間での SCU-DMA転送中には、絶対に SH2から A-BUSおよび
	     B-BUSにアクセスしないで下さい。

Q3
	SCU-DMA の転送終了を判定するのに、DMA終了割込み以外の方法は無いのか？
A3
	ありません。
	A-BUS, B-BUS 間での SCU-DMA実行中に 該当アドレスに SH2からのアクセスは
	禁止です。（理由は上記の通り)
	ソース又はディスティネーションが CPUバスの場合は、DMA転送が終了するまで
	SH2はウェイト状態となります。



3.変更履歴
3.1 Ver.1.05からVer.1.06への変更点
    SHC版
      ・CPU内部除算器使用時の割り込みマスク処理に関する不具合対応のために
        ライブラリのコンパイルオプションを-div=cpuとしました。
	GCC版の変更は特にありません。

3.2 Ver.1.06からVer.1.07への変更点
	SCU-DMA終了割込みルーチンを登録を、より安全な方法に変更しました。
	SHC版のみ、SHC Ver3.0G に変更しました。


********************************** end of file ********************************
